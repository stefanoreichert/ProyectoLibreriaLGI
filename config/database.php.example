<?php
/**
 * Archivo de configuración de base de datos - EJEMPLO
 * 
 * Copia este archivo como 'database.php' y configura tus credenciales
 */

// NO EDITAR ESTE ARCHIVO - Copiar como database.php

define('DB_HOST', 'localhost');
define('DB_NAME', 'biblioteca_lgi');
define('DB_USER', 'tu_usuario_mysql');
define('DB_PASS', 'tu_contraseña_mysql'); 
define('DB_CHARSET', 'utf8mb4');

// Configuración de conexión PDO
$dsn = "mysql:host=" . DB_HOST . ";dbname=" . DB_NAME . ";charset=" . DB_CHARSET;

$opciones_pdo = [
    PDO::ATTR_ERRMODE => PDO::ERRMODE_EXCEPTION,
    PDO::ATTR_DEFAULT_FETCH_MODE => PDO::FETCH_ASSOC,
    PDO::ATTR_EMULATE_PREPARES => false,
    PDO::MYSQL_ATTR_INIT_COMMAND => "SET NAMES utf8mb4"
];

try {
    $pdo = new PDO($dsn, DB_USER, DB_PASS, $opciones_pdo);
} catch (PDOException $e) {
    die("Error de conexión: " . $e->getMessage());
}
?>